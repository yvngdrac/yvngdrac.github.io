title: /home/yvngdrac
description: Security research and CTF writeups
url: https://yvngdrac.github.io
baseurl: ""

# Build settings
markdown: kramdown
highlighter: rouge
permalink: pretty

# Pagination
paginate: 10
paginate_path: "/page/:num/"

# Plugins
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-paginate

# Exclude files from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - .gitignore
  - README.md

# Default front matter
defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
      type: "pages"
    values:
      layout: "default"

# RSS feed
feed:
  path: feed.xml

# Social links
github_username: yvngdrac
